# Windows native 互換版

この作業ツリーは、`oned2424/job-copy-foot` の配布物を Windows native の Codex アプリで使えるようにするための互換版です。元の配布リポジトリや `main` ブランチは変更しません。

## なぜ必要か

配布元のスキルは WSL / macOS を前提にしており、実データを `/tmp` に限定し、POSIX の権限（`0600`）やシンボリックリンク検査を利用しています。一方、Windows native の Codex アプリでは WSL 実行環境を選ぶと起動に失敗する既知の不具合が発生したため、この端末では Windows native で安全に動かすための互換層が必要です。

## 変更方針

- 元の配布物には手を加えず、この作業ツリーでのみ互換処理を持つ。
- 一時データは Drive や同期フォルダではなく、`%LOCALAPPDATA%\\JobCopy\\tmp` に限定する。
- このフォルダは作成時に、現在の Windows ユーザー・SYSTEM・管理者だけが読めるアクセス権へ絞る。Desktop、Documents、Google Drive 配下には実データを置かない。
- Windows で再現できない POSIX 権限・シンボリックリンク依存は、同等の安全性を確認できるWindows向け検査へ置き換える。
- Google Drive のIDやクライアント設定は配布元の内容を保持し、手で書き換えない。

## 更新方法

配布元が更新されたときは、元の `main` をこの互換ブランチへ取り込み、Windows互換処理を再適用したうえで検証します。配布元の `main` へ互換対応を直接コミットしません。

## 現在の状態

- 元の配布物をローカルへ展開済み
- Windows専用の一時領域処理を実装済み。次は Node.js 向けの同等処理と、Python / Node の起動確認を実施する
